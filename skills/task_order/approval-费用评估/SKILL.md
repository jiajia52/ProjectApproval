---
skill_id: approval-费用评估
name: approval-费用评估-费用评估
description: 项目立项审批规则，聚合“费用评估”进程tab页下全部评审点。用于项目立项审批、远程项目审批和大模型审批建议生成时，对该tab页相关内容进行统一复核。
project_type: ALL
task_type: PROJECT_APPROVAL
object_type: PROJECT_TAB
spec_ids:
- builtin:任务单规则.xlsx
rules:
- 本次任务单费用 / 岗位、职级、人天、单价、费用: 具备岗位、职级、预计人天、人员单价、预计费用
- 历史任务单费用 / 历史任务单明细: 具备核定总金额
enabled: true
---
# Skill: approval-费用评估-费用评估

## 适用范围
- 进程tab页：费用评估
- 规则数量：2
- 模型维度：成本构成模型
- 业务维度：成本构成
- 涵盖评审点：本次任务单费用、历史任务单费用
- 适用品类：工作台开发及实施、产品运营、系统产品购买

## 规则依据
- 任务单规则.xlsx

## 审批目标
- 对“费用评估”tab页下的全部评审点、评审内容和规则进行一次性复核。
- 只基于该tab对应的材料、接口数据和映射后的项目文档做判断。
- 不跨tab扩展结论；跨tab要求只能作为提示，不能作为当前skill的否决依据。

## 结构化规则
### 本次任务单费用
- `R021` 岗位、职级、人天、单价、费用
  规则: 具备岗位、职级、预计人天、人员单价、预计费用
  适用品类: 工作台开发及实施、产品运营、系统产品购买
### 历史任务单费用
- `R022` 历史任务单明细
  规则: 具备核定总金额
  适用品类: 工作台开发及实施、产品运营、系统产品购买

## 审批口径（强制）
- 必须逐条覆盖本Skill下全部规则，不得跳项；不适用项标记为 N/A，并给出基于材料的理由。
- 若规则文本为空，按“不需要/规则表未要求”处理，不得自行补充新的强制否决条件。
- 优先使用远程接口原始返回、映射后的项目文档和审批落盘文件作为证据。
- 输出只保留复核后的结论、问题、建议，不输出思维链或内部推理过程。
- 当前Skill只负责本tab页，不替代其他7个tab的审查职责。

## 输出要求
- 输出 JSON 对象，至少包含 `review_point`、`status`、`summary`、`item_results`、`evidence`、`suggestion`。
- `review_point` 固定输出 `费用评估`。
- `item_results` 必须覆盖本Skill中列出的全部规则项。
