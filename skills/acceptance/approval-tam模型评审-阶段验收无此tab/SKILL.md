---
skill_id: approval-tam模型评审-阶段验收无此tab
name: approval-tam模型评审-阶段验收无此tab-TAM模型评审（阶段验收无此tab）
description: 项目立项审批规则，聚合“TAM模型评审（阶段验收无此tab）”进程tab页下全部评审点。用于项目立项审批、远程项目审批和大模型审批建议生成时，对该tab页相关内容进行统一复核。
project_type: ALL
task_type: PROJECT_APPROVAL
object_type: PROJECT_TAB
spec_ids:
- builtin:验收规则0324.xlsx
rules:
- 能力（竞争）模型 / 能力（竞争）模型: 具备能力模型（三者有一）
- 结果（财务/客户）模型 / 结果（财务/客户）模型: 具备结果模型（三者有一）
- 管理体系模型 / 管理体系模型: 具备管理模型（三者有一）
enabled: true
---
# Skill: approval-tam模型评审-阶段验收无此tab-TAM模型评审（阶段验收无此tab）

## 适用范围
- 进程tab页：TAM模型评审（阶段验收无此tab）
- 规则数量：3
- 模型维度：价值度量模型
- 业务维度：TAM 指标
- 涵盖评审点：能力（竞争）模型、结果（财务/客户）模型、管理体系模型
- 适用品类：工作台开发及实施、产品运营、系统产品购买、系统开发及运营

## 规则依据
- 验收规则0324.xlsx

## 审批目标
- 对“TAM模型评审（阶段验收无此tab）”tab页下的全部评审点、评审内容和规则进行一次性复核。
- 只基于该tab对应的材料、接口数据和映射后的项目文档做判断。
- 不跨tab扩展结论；跨tab要求只能作为提示，不能作为当前skill的否决依据。

## 结构化规则
### 能力（竞争）模型
- `R042` 能力（竞争）模型
  规则: 具备能力模型（三者有一）
有明确的指标标题、现状、受益部门、三年的计划目标以及测算依据。
  适用品类: 工作台开发及实施、产品运营、系统产品购买、系统开发及运营
### 结果（财务/客户）模型
- `R043` 结果（财务/客户）模型
  规则: 具备结果模型（三者有一）
有明确的指标标题、现状、受益部门、三年的计划目标以及测算依据。
  适用品类: 工作台开发及实施、产品运营、系统产品购买、系统开发及运营
### 管理体系模型
- `R044` 管理体系模型
  规则: 具备管理模型（三者有一）
有明确的指标标题、现状、受益部门、三年的计划目标以及测算依据。
  适用品类: 工作台开发及实施、产品运营、系统产品购买、系统开发及运营

## 审批口径（强制）
- 必须逐条覆盖本Skill下全部规则，不得跳项；不适用项标记为 N/A，并给出基于材料的理由。
- 若规则文本为空，按“不需要/规则表未要求”处理，不得自行补充新的强制否决条件。
- 优先使用远程接口原始返回、映射后的项目文档和审批落盘文件作为证据。
- 输出只保留复核后的结论、问题、建议，不输出思维链或内部推理过程。
- 当前Skill只负责本tab页，不替代其他7个tab的审查职责。

## 输出要求
- 输出 JSON 对象，至少包含 `review_point`、`status`、`summary`、`item_results`、`evidence`、`suggestion`。
- `review_point` 固定输出 `TAM模型评审（阶段验收无此tab）`。
- `item_results` 必须覆盖本Skill中列出的全部规则项。
