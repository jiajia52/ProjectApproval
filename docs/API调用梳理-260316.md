# API调用梳理（基于 620标签配置-最新版260316）

## 数据来源
- 接口标签配置: `data/620标签配置-最新版260316.xlsx`（立项B端）
- 评审规则矩阵: `data/立项大模型评审规则说明3-13.xlsx`
- 对齐结果输出: `runtime/api_rule_alignment_260316.json`

## 本次代码侧调整
- 规则矩阵自动识别优先 `立项大模型评审规则说明*.xlsx`，避免误选 `620标签配置*.xlsx`。
- 项目快照调用补齐并纳入兼容链路:
  - `project_scope_ops_get_scope -> /projectBaseInfo/getProjectScope`
  - `system_scope_okr -> /projectBaseInfo/eamapAndSystemOkr`
  - `organization -> /organizationalStructure/queryOrganizationNumber`
  - `organization_framework -> /projectOrgFramework/list/{projectId}`
- 项目范围数据合并顺序更新为:
  - `/project/range/list` -> `/projectBaseInfo/getProjectScope` -> `/projectBaseInfo/projectRange/list`
- 系统范围数据合并顺序更新为:
  - `/projectMicosInfo/getList` -> `/projectBaseInfo/eamapAndSystemOkr`
- 组织成员数据合并来源更新为:
  - `organization_flag_0` + `organization_flag_1` + `organization` + `organization_framework`

## 620 与 3-13 一致性复核结论
- 620 立项B端维度:
  - 业务类别 6 个
  - 业务子类 20 个
  - 进程标签 8 个
  - 子标签 17 个
- 3-13 规则矩阵维度:
  - 规则 41 条
  - 品类 22 个
  - 评审点 19 个
- 核心差异:
  - 评审点命名差异: `专业技术领域评审` vs `专业领域评审`
  - 括号风格差异: `能力(竞争)模型` vs `能力（竞争）模型`，`结果(财务/客户)模型` vs `结果（财务/客户）模型`
  - 620无对应评审点: `长尾费用`
  - 业务子类命名差异: `系统开发及实施` vs `工作台开发及实施`，`数据订阅及购买` vs `数据订阅购买`，以及运维子类命名差异

## 已落地的命名对齐
- 分类别名:
  - `系统开发及实施 -> 工作台开发及实施`
  - `系统运维(一、二线) -> 大一线运维`
  - `系统运维(三线) -> 三线运维`
  - `系统运维(产品维保) -> 产品维保`
  - `数据订阅及购买 -> 数据订阅购买`
- 评审点别名:
  - `专业技术领域评审 -> 专业领域评审`
  - `能力(竞争)模型 -> 能力（竞争）模型`
  - `结果(财务/客户)模型 -> 结果（财务/客户）模型`
